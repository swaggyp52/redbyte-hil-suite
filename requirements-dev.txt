# RedByte GFM HIL Suite — development / test dependencies
# Install with:  pip install -r requirements-dev.txt
#
# This extends requirements.txt.  Run both:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

pytest>=7.0.0
pytest-cov>=4.0.0
playwright>=1.40.0

# Optional — for Excel import tests
openpyxl>=3.1.0

# Linting / formatting
black
isort
mypy
